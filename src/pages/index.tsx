import type { NextPage } from 'next';

import DiscountSection from '@/components/DiscountSection';

const Home: NextPage = () => {
  return (
    <div>
      <DiscountSection />
    </div>
  );
};

export default Home;
